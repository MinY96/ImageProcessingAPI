# Optional local NVIDIA/PyTorch diffusion stack for Synthetic NG Generator.
# Install after requirements.txt.
#
# Pinned to diffusers 0.39.0 so this file can coexist in the same venv with
# requirements-openvino-diffusion.txt / optimum-intel 2.2.0.
diffusers==0.39.0
huggingface-hub>=0.34,<1.22
transformers>=4.51,<5.6
accelerate>=1.5,<2
safetensors>=0.5,<1
sentencepiece>=0.2,<1
