# Optional local diffusion stack for Synthetic NG Generator.
# Install after requirements.txt so the project keeps a lightweight OpenCV-only mode.
diffusers>=0.40,<0.50
transformers>=4.51,<6
accelerate>=1.5,<2
safetensors>=0.5,<1
sentencepiece>=0.2,<1
